# Endpoint del workapp (NestJS) que sirve los datos de Google Sheets parseados.
# OJO: este archivo también lo usa `npm run build` (Pages) — debe quedar la URL de Azure.
# Para desarrollo local contra un workapp local, usar .env.development.local (gitignored).
VITE_SHEETS_API_URL=https://vestadnelsonmbackend-fhgcfkepexh8f6ff.centralus-01.azurewebsites.net/api/flujo-sheets
